// use of variables to store the value temporary and use over the program

let name = "Jake";
console.log(name);

// Rules for variable declaration

// 1. Can not be reserved keyword.
// 2. should be meaningful
// 3. cannot  start with a number
// 4. cannot contain a space or hyphen
// 5. Are case-sensitive

// Constants

let interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// value of variable change but constant not be changes.

const rate = 12;
//rate = 1233;
console.log(rate);
